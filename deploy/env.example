# Copy to deploy/.env (gitignored) and adjust paths if needed.
DEPLOY_ENV=dev
DEPLOY_HOST=172.30.47.194
DEPLOY_USER=aziron
DEPLOY_PATH=/home/aziron/workspace/aziron-UIUX
APP_PORT=8080

# Optional: rsync local tree to the VM before docker (set to 1 to enable)
# RSYNC_BEFORE_DEPLOY=1
